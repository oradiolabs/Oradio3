# Oradio uses keyfile connection profiles only, stored in /etc/NetworkManager/system-connections.
# Restricting the settings plugin list drops the netplan and ifupdown plugins, neither of which
# has anything to read: /etc/netplan is empty and /etc/network/interfaces does not exist.
[main]
plugins=keyfile